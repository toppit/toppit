import React from 'react';
import {Container, Header} from 'semantic-ui-react';

const TopicDetailed = (props) => (
  <Container>
    <Header as='h2'>Hello World</Header>
  </Container>
);

export default TopicDetailed;